apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "nginx-app.fullname" . }}-content
  labels:
    {{- include "nginx-app.labels" . | nindent 4 }}
data:
  index.html: |
    {{- .Values.content | nindent 4 }}
